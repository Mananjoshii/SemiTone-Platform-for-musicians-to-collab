<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Band Member Profile</title>
</head>
<body>
    <div class="container mt-5">
        <h1>Welcome, <%= user.name %>!</h1>
        <h3>Role: Band Member</h3>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>About You:</strong> <%= user.description %></p>

        <h4>Bands You Are Part Of:</h4>
        <ul>
            <% // Fetch and loop through bands associated with the user %>
            <% bands.forEach(function(band) { %>
                <li><%= band.name %> - <%= band.description %></li>
            <% }); %>
        </ul>

        <a href="/logout" class="btn btn-danger">Logout</a>
    </div>
</body>
</html>
